# Docker Notes

This folder holds container examples for the MCP walkthrough.

## First pass

- keep the image simple
- run the canonical `public-api-demo` server
- evolve this into a production-ready image once the final remote transport is fixed
